require "spec_helper"

describe MaterialServiceClient do
  it "has a version number" do
    expect(MaterialServiceClient::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
